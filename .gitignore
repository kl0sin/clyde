.build/
.swiftpm/
DerivedData/
*.xcodeproj/xcuserdata/
.superpowers/
.DS_Store

# Stray Swift incremental build artifacts that escape .build/
*.d
*.o
*.swiftdeps
*.swiftdeps~

# Claude Code workflow / skills — personal dev environment, not part of
# the published app source.
.claude/
.agents/
skills-lock.json

# Local release artifacts (build.sh outputs).
build/

# Apple Developer credentials — must NEVER be committed.
*.p12
*.cer
*.certSigningRequest
sparkle-private-key
*.keychain-db
