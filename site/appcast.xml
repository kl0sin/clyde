<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle"
     xmlns:dc="http://purl.org/dc/elements/1.1/"
     version="2.0">
    <channel>
        <title>Clyde</title>
        <link>https://kl0sin.github.io/clyde/appcast.xml</link>
        <description>Auto-update feed for the Clyde menu bar app.</description>
        <language>en</language>
        <item>
            <title>Version 0.7.0</title>
            <pubDate>Thu, 27 Aug 2026 11:54:25 +0000</pubDate>
            <sparkle:version>22</sparkle:version>
            <sparkle:shortVersionString>0.7.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>Clyde's hook coverage stopped growing while Claude Code kept getting more autonomous, and the gap showed up exactly where it hurts: agent teams, batches of parallel tool calls, slash commands driving long unattended runs. This release closes it. The session row stops saying "Working" and starts saying what is actually happening — which command is running, which worktree it is running in, how many tools are in flight, and what Claude said when it finished. It also fixes the one place where Clyde's model of a session was provably wrong: subagents were tracked through the tool call that dispatched them, so a backgrounded agent vanished from the panel while it carried on working. Clyde's hook script upgrades itself on launch; nothing to reinstall.</p>
<ul><li><strong>Subagents no longer disappear from the panel while they're still working.</strong> Clyde tracked each in-flight subagent through the tool call that dispatched it, which held up right until Claude started running agents in the background — those calls return the moment the agent is handed off, so Clyde tore the row out of the panel while the agent carried on working. On a real session the gap measured five seconds; for a long-running agent it's minutes of the panel claiming nothing is in flight. Clyde now follows the agent's own identity instead, so a row appears when the agent is dispatched and disappears when the agent actually finishes.</li><li><strong>Parallel tool calls now show up as parallel.</strong> Claude often runs several tools at once. Clyde used to track only one of them, so the row showed whichever call happened to register last and went blank the moment the first of them finished — even with three still running. Each call is now tracked separately: the row reads <code>3 tools · 0:04</code> while a batch is in flight and only clears when the last one is genuinely done.</li><li><strong>The running slash command is named on the row.</strong> Start a long <code>/loop</code> or <code>/code-review</code> and the session shows an amber <code>/loop</code> badge next to the project name instead of an anonymous "Working", so you can tell at a glance which of your sessions is grinding through what.</li><li><strong>Worktree sessions are labelled.</strong> A session working inside a git worktree gets a green badge with the worktree's name, instead of showing an opaque <code>.claude/worktrees/…</code> path. The badge drops the moment the session leaves the worktree.</li><li><strong>Idle sessions show Claude's last reply.</strong> Instead of repeating the project path, an idle session's second line now previews the first line of what Claude last said.</li><li><strong>Skill, Workflow and Artifact calls are named.</strong> These three used to render as a bare tool name; the row now shows which skill, which workflow and which page.</li><li><strong>Agent-team teammates that go idle now show as idle.</strong> When a teammate on an agent team is about to go idle, its row in the panel settles instead of pretending work is still in progress — the little mascot stops animating, the duration stops counting, and the label reads <code>idle</code>. The row stays put, so you can still see who is on the team and what they were doing. This is intentionally a quiet state and not an alert: Clyde won't light up the "needs your input" badge for it until we know how often the event really fires.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.7.0/Clyde-0.7.0.dmg"
                length="5559535"
                type="application/octet-stream"
                sparkle:edSignature="flXlrSc5JK9RP1KBqBbeAbxNiipgmprAdzYkK7W3mZik0xk5dzOagEvcJJCpeRlA+a0vQcIsBxpj6i9BMbFvBw==" />
        </item>
        <item>
            <title>Version 0.6.0</title>
            <pubDate>Tue, 09 Jun 2026 20:19:41 +0000</pubDate>
            <sparkle:version>21</sparkle:version>
            <sparkle:shortVersionString>0.6.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>Clyde can now start itself. A new "Launch at login" toggle in Settings → General registers Clyde with macOS so it's already sitting in your menu bar when you sign in — no more remembering to launch it before kicking off a Claude session.</p>
<ul><li><strong>Launch at login.</strong> Settings → General gains a Startup section with a single switch. Flip it on and macOS starts Clyde automatically at sign-in, using the system's native login-items registration (<code>SMAppService</code>, macOS 13+) — the same mechanism you see under System Settings → General → Login Items, where Clyde shows up and can be managed too. If macOS requires your explicit approval for the registration, Clyde shows an inline "Approval needed" hint with a button that jumps straight to the Login Items pane instead of pretending the toggle worked. State stays in sync both ways: flip it in System Settings and Clyde's toggle reflects the change the next time you open Settings.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.6.0/Clyde-0.6.0.dmg"
                length="5533262"
                type="application/octet-stream"
                sparkle:edSignature="mxSrJBmAul8jJNeDCvsQVEIAKfkEE410cPq5sURzmas+aWdg1N393tuuXocbE/ZTqAUHkWSBfr2JCfhXoTs6DA==" />
        </item>
        <item>
            <title>Version 0.5.3</title>
            <pubDate>Thu, 21 May 2026 12:08:41 +0000</pubDate>
            <sparkle:version>20</sparkle:version>
            <sparkle:shortVersionString>0.5.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>Iteration on the cleat advisory banner introduced in v0.5.0 — it now refreshes live as you toggle cleat's host hook bridge, splits the message into a readable title + body, surfaces the fix command as a one-click copyable chip, and gains an × button if you want to silence it for the session.</p>
<ul><li><strong>The cleat advisory banner is now a proper card.</strong> Title-bold headline ("Cleat hook bridge is off"), softer body underneath, and the fix command lives in a discrete copyable chip — click it to drop <code>cleat config --enable hooks</code> straight into your clipboard, with a brief ✓ confirmation. The old inline-backtick rendering was hard to read against the orange banner; the chip has a proper rounded background, clipboard icon, and tap-to-copy affordance.</li><li><strong>Banner appears and disappears live.</strong> Toggling cleat's <code>hooks</code> capability via <code>cleat config --enable/--disable hooks</code> now reflects in the panel within ~0.3s instead of waiting up to 60s for the safety-net timer. Implemented via a pair of FSEvents watchers on <code>~/.config/cleat/</code> — one on the directory (catches the disable path, which recreates the file) and one on the config file itself (catches the enable path, which writes in place — the dir watcher alone would miss it).</li><li><strong>Dismiss button on advisory banners.</strong> A small × in the banner's top-right snoozes the cleat advisory for the current Clyde session. The banner reappears on relaunch, or if you toggle the cap on and then off again — handy for "I'll deal with it later" without losing the reminder permanently. Critical hook-health banners (script missing, outdated, etc.) stay non-dismissable since they break tracking entirely. Dismiss state lives in memory only — no settings to clear, no persisted state to debug.</li><li><strong>No more misleading "Click to open Settings" on the cleat banner.</strong> Settings can't run <code>cleat config</code> for you, so the affordance pointed nowhere useful. Actionable banners (the ones whose fix lives in Settings) keep their click target; advisories render as informational rows. Internal: hook script bumped to v27 (no behaviour change — just version-stamped with the latest fix from v0.5.2).</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.5.3/Clyde-0.5.3.dmg"
                length="5493076"
                type="application/octet-stream"
                sparkle:edSignature="MS5b2MQKP4Pjt5gWiMtCw4VPiaOu+D3cYnKk4LjOmp7kepUaiBcjDHweIm6w86ftHVN/gEN7lXsNmkPZQSc+Dg==" />
        </item>
        <item>
            <title>Version 0.5.2</title>
            <pubDate>Thu, 21 May 2026 07:32:35 +0000</pubDate>
            <sparkle:version>19</sparkle:version>
            <sparkle:shortVersionString>0.5.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>Follow-up to v0.5.1: the "Needs Input" badge no longer false-positives on every routine reply. Turns out <code>Notification("Claude is waiting for your input")</code> isn't an attention signal — Claude fires it after every <code>Stop</code> in bypass-permissions mode as a plain idle marker, so v0.5.1 lit the badge up on every turn including the most boring "here's your answer" ones.</p>
<ul><li><strong>"Needs Input" badge no longer fires on idle replies.</strong> v0.5.1 mapped <code>Notification("Claude is waiting for your input")</code> onto the attention indicator on the theory that it's Claude explicitly saying it's waiting for you. In practice Claude fires that exact message after every <code>Stop</code> in bypass-permissions mode (cleat's default), including routine turns where nothing actually needs your attention — so the badge ended up showing on every reply. v0.5.2 drops that string from the match. The permission-flavoured forms ("needs your permission to use …") still trigger the badge as a safety net for non-bypass builds where they could be the only attention surface. Existing stale badges from v0.5.1 clear themselves the next time you reply in the affected session — no manual cleanup. Hook script bumped to v27.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.5.2/Clyde-0.5.2.dmg"
                length="5429375"
                type="application/octet-stream"
                sparkle:edSignature="InsfeyPbHzSczdENL6SBoCgQZcDeJ4MganaoHo9MrwB/pn9Rua2DPrL8iJ+tOVwoR8ExJWcTkdgAzsaIsIK0Aw==" />
        </item>
        <item>
            <title>Version 0.5.1</title>
            <pubDate>Thu, 21 May 2026 07:14:06 +0000</pubDate>
            <sparkle:version>18</sparkle:version>
            <sparkle:shortVersionString>0.5.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>Quick follow-up to v0.5.0 — the "needs your input" badge now lights up for sessions running in <code>--dangerously-skip-permissions</code> mode (most commonly <a href="https://github.com/cleatdev/cleat">cleat</a>-sandboxed ones), which previously sat dark in the panel even when Claude was waiting on you.</p>
<ul><li><strong>"Needs your input" badge now fires in bypass-permissions mode.</strong> Claude Code doesn't fire <code>PermissionRequest</code> when running under <code>--dangerously-skip-permissions</code> (cleat's default, and the whole point of the sandbox). It fires <code>Notification</code> with <code>message: "Claude is waiting for your input"</code> instead. Clyde used to treat every Notification as log-only; hook v26 now matches that message (plus the alternate "Claude needs your permission to use …" form) and surfaces the same orange attention indicator you'd see for a regular permission gate. The badge drops the moment you reply — <code>UserPromptSubmit</code> now sweeps the attention flag too, so it doesn't linger on plan-mode or pure-text turns where no tool call fires immediately afterwards.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.5.1/Clyde-0.5.1.dmg"
                length="5429109"
                type="application/octet-stream"
                sparkle:edSignature="GG71m8oBQ0Olp/wbAzhZGYAt+RCDMcsZMlyYCujA3xoXzUt45Mh9w7BkSDWGoQ6UyI70xF9S3xRYztCpvq5NAw==" />
        </item>
        <item>
            <title>Version 0.5.0</title>
            <pubDate>Wed, 20 May 2026 19:14:27 +0000</pubDate>
            <sparkle:version>17</sparkle:version>
            <sparkle:shortVersionString>0.5.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>First-class support for Claude Code sessions running inside <a href="https://github.com/cleatdev/cleat">cleat</a> — the Docker sandbox that lets you run <code>claude --dangerously-skip-permissions</code> without risking your host — plus a small UI fix that stops the progress and runtime badges from getting visibly clipped on long project names.</p>
<ul><li><strong>Now tracks sessions running inside <a href="https://github.com/cleatdev/cleat">cleat</a>.</strong> Cleat sandboxes Claude Code in Docker so it can run with full autonomous permissions without touching your host — same productivity, zero blast radius. Clyde now detects cleat-sandboxed sessions automatically and renders them in the panel like any native session, with a small cyan <strong>cleat</strong> badge next to the project name so you can tell them apart. Working directories show as the real host path (no more <code>/workspace</code> mystery), and liveness tracking is wired to the cleat process on the host so sessions age out correctly when you close your cleat terminal. If you have cleat on your PATH but haven't enabled its host hook bridge yet, Clyde surfaces a one-line banner pointing at <code>cleat config --enable hooks</code> so sandboxed sessions actually reach the panel. Big thanks to the cleat team for shipping a tool that makes autonomous Claude actually safe to leave running overnight — go check it out at <a href="https://github.com/cleatdev/cleat">github.com/cleatdev/cleat</a>. Hook script bumped to v25; existing installs auto-upgrade on Clyde launch.</li><li><strong>Plan and cleat badges no longer get clipped on narrow rows.</strong> SwiftUI was happy to give the project-name label all available horizontal space and squeeze the capsules past their content box, so the progress icon or the "cleat" tag would render visibly cut off on the right edge for projects with longer names. Both badges now refuse to compress; the name takes the ellipsis instead.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.5.0/Clyde-0.5.0.dmg"
                length="5428520"
                type="application/octet-stream"
                sparkle:edSignature="69kJMzzzFXrGmYfcyus4viGmohVMD+twuVeoAVwKx5Pk/dC9lUMKibZ2kLJ//ll4QDF6arF1UcXaMj7FVIH7Dg==" />
        </item>
        <item>
            <title>Version 0.4.0</title>
            <pubDate>Thu, 14 May 2026 07:32:37 +0000</pubDate>
            <sparkle:version>16</sparkle:version>
            <sparkle:shortVersionString>0.4.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>Parallel subagents land in the panel, plus a UI polish pass and a fix for the long-standing "Home" mislabel on sessions Clyde discovered through <code>pgrep</code>.</p>
<ul><li><strong>Parallel subagents in the panel.</strong> When Claude fans out to several subagents at once via the built-in <code>Agent</code> tool, the session row now flips its second line to e.g. <code>5 agents · 0:12</code> and renders a lavender-accented list of each in-flight subagent underneath — type and live duration on one line, the short Claude-supplied description on the next. Each row gets a static pixel-art mini-mascot so the affordance reads as part of the same visual family as the main indicator. Lists longer than three collapse behind a <code>+N more agents</code> tap; the list auto-collapses again once activity drops to three or fewer. Defensive 30-minute GC drops zombies left behind by crashed parents, and the legacy single-subagent indicator keeps working for sessions started before this build.</li><li><strong>Real project names for sessions discovered through <code>pgrep</code>.</strong> Clyde used to mislabel any session whose <code>.claude/settings</code> heuristic only matched the global <code>~/.claude/settings.json</code> as "Home". The new detection asks <code>lsof -d cwd</code> for the process's actual working directory first, so a <code>claude</code> running in <code>~/_Projects/pulse</code> shows up as "pulse" instead. The settings-based heuristic stays as a fallback for older Claude builds that chdir'd to <code>/</code>, and the global <code>~/.claude/settings.json</code> is now explicitly rejected from that fallback so it can no longer impersonate a real project.</li><li><strong>UI polish pass on the session row.</strong> Design tokens for text colours unified across the row, hover states gain proper feedback, and the row no longer jumps vertically by a pixel when the rename pencil appears on hover. The subagent block spans the row's full width (so per-agent durations right-align with the trailing "Working · Ns" indicator), the mascot and right-side status both anchor to the top of the row instead of drifting down as the subagent list grows, and all secondary text in the block matches the existing 10pt monospaced typography of the tool/path indicator.</li><li><strong>Hook script v21.</strong> Claude Code emits <code>tool_name="Agent"</code> on subagent dispatch (the older <code>"Task"</code> name no longer ships). The Pre/Post lifecycle, summary line, and defensive cleanups in <code>clyde-hook.sh</code> now match on <code>Agent</code> (with <code>Task</code> retained as a defensive fallback). Existing installs auto-upgrade on Clyde launch; no manual hook reinstall needed.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.4.0/Clyde-0.4.0.dmg"
                length="5402501"
                type="application/octet-stream"
                sparkle:edSignature="dyDxtSt8PjXYIR1eT05svcyZ0m8BvPxcLGBtu4hF0SUnHD7ljr8dTm5Eq+hs9SU+WRKMVSx0biJf/PCinPDQBw==" />
        </item>
        <item>
            <title>Version 0.3.1</title>
            <pubDate>Thu, 30 Apr 2026 18:33:32 +0000</pubDate>
            <sparkle:version>15</sparkle:version>
            <sparkle:shortVersionString>0.3.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>Onboarding plus accessibility plus a plan-badge fix on top of v0.3.0. Same feature surface — same panel, same hooks, same telemetry — just easier to discover and to use, regardless of how you read the screen.</p>
<ul><li><strong>First-run coachmark tour.</strong> After the welcome modal Clyde now walks you through the four things that aren't obvious at a glance — what each session row shows and means, the live <code>Edit · MyFile.swift · 3s</code> tool/plan line that ticks while Claude works, the snooze button for muting alerts during meetings, and the global <code>⌃⌘C</code> hotkey for opening or hiding the panel from anywhere. Skippable mid-tour, replayable from Settings → General, and silent for anyone upgrading from a Clyde version that didn't have it.</li><li><strong>Accessibility pass.</strong> Every interactive control in Clyde now announces itself meaningfully under VoiceOver — session rows speak their name, status, current tool and plan progress as one breath, the menu-bar capsule reads its live count, and tooltips, snooze remaining time, and onboarding cards all carry semantic labels. With Reduce Motion enabled in System Settings, Clyde stops every auto-running animation: the mascot freezes on its first frame, status pills no longer pulse, and slide transitions become opacity crossfades. Drag-and-drop and color transitions stay intact.</li><li><strong>Completed plan badges no longer linger.</strong> When Claude finishes a multi-step plan (the green <code>✓ N/N</code> badge), the badge now disappears the next time you submit a prompt instead of sticking around through every subsequent unrelated turn. Partial plans still persist across turns so the progress badge keeps tracking when you type "continue" mid-execution — only fully-completed badges get cleared.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.3.1/Clyde-0.3.1.dmg"
                length="5283448"
                type="application/octet-stream"
                sparkle:edSignature="Pm67hL1CQgDBg1biiz0H7KvLxadZJXhBqFHIF8WC1sDmaWkGAkpfQb68EZoeNH8LhVMJcFZlp8/P/Bs/FXy1BQ==" />
        </item>
        <item>
            <title>Version 0.3.0</title>
            <pubDate>Wed, 29 Apr 2026 12:41:00 +0000</pubDate>
            <sparkle:version>14</sparkle:version>
            <sparkle:shortVersionString>0.3.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>Richer session telemetry. Clyde stops at "is Claude busy?" and starts answering "what is Claude actually doing?" — every session row now narrates the active tool with a live duration, and surfaces plan-then-execute progress when Claude maps out a multi-step run.</p>
<ul><li><strong>The session row now shows what Claude is actually doing.</strong> When Claude calls a built-in tool — <code>Edit</code>, <code>Write</code>, <code>Read</code>, <code>Bash</code>, <code>Glob</code>, <code>Grep</code>, <code>Task</code>, <code>WebFetch</code>, <code>WebSearch</code> — the second line of the row swaps from the project path to e.g. <code>Edit · SessionRow.swift · 3s</code> with a spring slide animation, ticking the duration live. When the tool finishes the path slides back in. TodoWrite and MCP tools show just the tool name (no summary), so the indicator is silent only on truly unidentifiable activity. Powered by a new hook event capture (<code>-tool</code> state file) — no new IPC.</li><li><strong>Plan-then-execute progress on the session row.</strong> When Claude maps out tasks via <code>TaskCreate</code> (the planning tool), Clyde shows a small <code>📋 N/M</code> badge with a progress bar next to the session name. The bar fills as Claude completes tasks and switches to a green <code>✓ N/N</code> when the plan is done. The badge persists across turns, so a long plan that Claude works on over several "continue" prompts keeps tracking — no flicker, no reset. Right-click "Reset session state" wipes the badge if it ever lingers after a plan changes direction.</li><li><strong>"Reset session state" now actually resets.</strong> The per-session reset action in the row's right-click menu used to leave behind <code>-error</code>, <code>-subagent</code>, and <code>-tool</code> markers — only <code>-info</code> and <code>-busy</code> were cleared. It now wipes every hook-written marker plus any pending attention event, so the manual escape hatch works whether the wedged state is a stale plan badge, a stuck "Working" pill, or a phantom "Needs Input" flag.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.3.0/Clyde-0.3.0.dmg"
                length="5164893"
                type="application/octet-stream"
                sparkle:edSignature="T9v21CfuzKyHMjlsbouldaAWNoTBWs03f3uEa6WNEMdpIO2PMwerdAF1PW3vWXqZ1ryjJymdLIBUtH646zGCCA==" />
        </item>
        <item>
            <title>Version 0.2.3</title>
            <pubDate>Wed, 29 Apr 2026 08:36:30 +0000</pubDate>
            <sparkle:version>13</sparkle:version>
            <sparkle:shortVersionString>0.2.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>Small-but-real bugfix and timeline polish pass on top of v0.2.2.</p>
<ul><li><strong>No more blank Settings window on launch.</strong> macOS state restoration was occasionally resurrecting SwiftUI's placeholder <code>Settings</code> scene (Clyde's real Settings live in a window managed by <code>AppDelegate</code>), which surfaced as an empty gray window appearing at random app launches. <code>NSQuitAlwaysKeepsWindows</code> is now <code>false</code> in <code>Info.plist</code> so Cocoa skips restoration entirely — the menu-bar app owns its windows explicitly anyway.</li><li><strong>Auto-compact and <code>/clear</code> now show up in the Activity timeline.</strong> Previously the second <code>SessionStart</code> that Claude Code fires after an auto-compact (same PID, fresh context) slipped past the diff silently, so the timeline went quiet right when the most interesting thing was happening. Clyde now tracks the hook <code>source</code> field per session and emits a dedicated "Session compacted" entry whenever the source flips to <code>compact</code> or <code>clear</code>.</li><li><strong>Hook log records the SessionStart source.</strong> <code>clyde-hook.sh</code> (v16) now appends <code>source=startup|resume|clear|compact</code> to the always-on hook log, so future investigations don't need to correlate <code>SessionStart</code> lines with <code>PreCompact</code>/<code>PostCompact</code> to tell apart a real <code>--resume</code> from an in-place auto-compact restart.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.2.3/Clyde-0.2.3.dmg"
                length="5082792"
                type="application/octet-stream"
                sparkle:edSignature="kpl7t3n9TusV2qYF6Wbpnt2sn1L08eVrcZYGruv4IOwFhUkv9Kb2vMmvx7tJnABOrruBuHTaWyf1TcXQWV0HBA==" />
        </item>
        <item>
            <title>Version 0.2.2</title>
            <pubDate>Mon, 27 Apr 2026 12:54:12 +0000</pubDate>
            <sparkle:version>12</sparkle:version>
            <sparkle:shortVersionString>0.2.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>Quality-of-life pass on top of the v0.2.1 signed release. No new product features — just polish across the install surface, the update dialog, and one real bugfix.</p>
<ul><li><strong>Homebrew cask published.</strong> <code>brew tap kl0sin/tap &amp;&amp; brew install --cask clyde</code> now works, served from <a href="https://github.com/kl0sin/homebrew-tap">kl0sin/homebrew-tap</a>. The release workflow stamps the cask on every signed release, so the tap stays in sync automatically.</li><li><strong>Sparkle release notes render as HTML.</strong> The update dialog now shows formatted bullets, headings, and bold text instead of raw markdown punctuation. The release pipeline pipes the CHANGELOG section through a small markdown→HTML converter before writing it into the appcast <code>&lt;description&gt;</code>.</li><li><strong>Keyboard Shortcuts section in Settings.</strong> Settings → General now surfaces the discoverable shortcuts (<code>⌃⌘C</code> to toggle the panel, <code>⌘,</code> for Settings, <code>⌘Q</code> to quit), and the same list landed in the README so newcomers can find them without launching the app.</li><li><strong>Phantom session rows from recycled PIDs are gone.</strong> When macOS reused a dead Claude PID for an unrelated long-lived binary, <code>discoverPIDs</code> was only checking liveness — never the argv[0] identity — so the recycled PID kept showing up as a Claude session in Clyde's UI. The discovery path now mirrors the identity check used for <code>-busy</code> markers, with regression coverage in <code>ProcessMonitorTests</code>.</li><li><strong>Smoke-test doc covers the permission-denied path.</strong> Scenario 4b walks through <code>PermissionRequest → PermissionDenied</code>, where <code>events/&lt;sid&gt;.json</code> is dropped instantly instead of lingering on a tool call the user explicitly rejected.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.2.2/Clyde-0.2.2.dmg"
                length="5080346"
                type="application/octet-stream"
                sparkle:edSignature="xuOUHcNeDiI1y26+h869VVSFrIpwBzccplRMa7Dnzar2HkGKXqpXiYJvz1VSaLYUi6Y2/2ASiOZjNbCrlQrrBQ==" />
        </item>
        <item>
            <title>Version 0.2.1</title>
            <pubDate>Sun, 26 Apr 2026 07:21:40 +0000</pubDate>
            <sparkle:version>11</sparkle:version>
            <sparkle:shortVersionString>0.2.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>First signed and notarized release. No product changes.</p>
<ul><li><strong>Gatekeeper-friendly.</strong> Clyde is now signed with a Developer ID certificate and notarized by Apple. First launch no longer shows the "unidentified developer" warning — just open the DMG and drag to Applications.</li><li><strong>Sparkle auto-updates activated.</strong> The appcast is now live, so future versions will install themselves in the background instead of requiring a manual DMG download.</li></ul>
            ]]></description>
            <enclosure
                url="https://github.com/kl0sin/clyde/releases/download/v0.2.1/Clyde-0.2.1.dmg"
                length="5080024"
                type="application/octet-stream"
                sparkle:edSignature="nGfw3jwar15flHf3nbc/Os5csUkBSCWoUy6L5BOB9PGFPaqJGUP72kx5426wUPyMzXOo1AoyVIs6bNUbcb7WDg==" />
        </item>
        <!-- New <item> entries are inserted here by scripts/release/update-appcast.sh -->
    </channel>
</rss>
